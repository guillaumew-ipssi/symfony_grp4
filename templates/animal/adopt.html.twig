{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}

    <p>Vous souhaitez adopter un animal ? Votre décision est mûrement réfléchie ? 
    Remplissez ce formulaire pour établir un contact avec le refuge. 
    Nous vous conseillons d’indiquer votre numéro de téléphone 
    pour qu’on vous rappelle plus rapidement.</p>

    {{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}
    <div class="container">    
        <form>
        <div class="form-group">
            {{ form_row(form.firstname) }}
        </div>
        <div class="form-group">
            {{ form_row(form.lastname) }}
        </div>
         <div class="form-group">
            {{ form_row(form.adress) }}
        </div>
         <div class="form-group">
            {{ form_row(form.city) }}
        </div>
         <div class="form-group">
            {{ form_row(form.zipcode) }}
        </div>
        <div class="form-group">
            {{ form_row(form.mobile) }}
        </div>
        <button type="submit" class="btn btn-primary">Valider</button>
        </form>
    </div>
    {{ form_end(form) }}
{% endblock %}