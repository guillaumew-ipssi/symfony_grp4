<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('home')}}">Petsland</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" href="{{path('animal')}}">Animaux</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('product')}}">Produits</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('blog')}}">Blogs</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('donation')}}">Faire un don</a>
				</li>
			</ul>
		</div>
		<div style="width: 7em;" class="d-flex">
			<a class="ml-4 text-white" href="{{ path('panier') }}">Mon Panier</a>
		</div>
		<div style="width: 9em;" class="d-flex">
			<a class="ml-4 text-white" href="{{ path('order_list') }}">Mes commandes</a>
		</div>
		<div class="d-flex">				
			{% if is_granted('IS_AUTHENTICATED_FULLY') %}
				<a class="btn btn-danger" href="{{path('app_logout')}}">Se déconnecter</a>	
			{% else %}
				<a class="btn btn-success text-white" href="{{path('app_login')}}">Se connecter</a>
			{% endif %}
		</div>
	</div>
</nav>
